export const baseUrlApi = (url: string) => `/api/${url}`;
export const accountUrlPrefix = (url: string) => `/api/accounts${url}`;
export const codeInfoUrlPrefix = (url: string) => `/api/code-info${url}`;
